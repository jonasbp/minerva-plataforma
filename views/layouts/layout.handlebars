<!DOCTYPE html>
<html>

<head>
    <title>Login app</title>
    <link rel="stylesheet" href="/css/bootstrap.css" />
    <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
    <div class="container">
        <nav>
            <ul class="menu ">
                {{#if user}}
                <li role="presentation"> <a href="/users/logout"> Logout <br> <br> </a></li>
                {{else}}
                <li role="presentation"> <a href="/"> Sobre <br> <br> <br> </a> </li>

                <li role="presentation"> <a href="/users/login"> Login <br> <br> </a></li>

                <li role="presentation"> <a href="/users/register"> Cadastrar-se <br> </a></li>
                {{/if}}

            </ul>
        </nav>

    </div>
    <div class="row">
        <div class="col-lg-12">
            {{#if success_msg}}
            <div class="alert alert-success"> {{success_msg}}</div>
            {{/if}}

            {{#if error_msg}}
            <div class="alert alert-danger"> {{error_msg}}</div>

            {{/if}}

            {{#if error}}
            <div class="alert alert-danger"> {{error}}</div>
            {{/if}}

            {{{body}}}
        </div>

    </div>


</body>

</html>